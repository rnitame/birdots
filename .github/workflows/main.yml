on:
  pull_request:
    branches:
      - main

jobs:
  main:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run chezmoi
        env:
          CI: "true"
        run: bash -c "$(curl -fsLS get.chezmoi.io)" -- init rnitame/birdots --branch ${{github.head_ref}} --apply

      - name: Run tests
        run: |
          brew install bats-core

          bats -r tests
